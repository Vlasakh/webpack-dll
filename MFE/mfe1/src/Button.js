import React from 'react';

// create a button component
const Button = () => {
  const onClick = () => {};

  return (
    <div>
      <button onClick={onClick}>Button from MFE1</button>
    </div>
  );
};

export default Button;
